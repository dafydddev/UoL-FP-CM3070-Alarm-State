namespace Entities.Objectives
{
    // Which minigame an objective's hack presents.
    public enum HackKind
    {
        Pipes, // the pipe circuit puzzle; the facility waits while it is open
        Sequence, // the key order; the facility carries on around it
    }
}
